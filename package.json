{
  "name": "shopify-jwt-auth-verify",
  "version": "1.0.10",
  "description": "A single function to be used as a verification for the new JWT app auth, used on the backend to verify FE requests incoming.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --collectCoverage",
    "test:watch": "jest --watchAll"
  },
  "prepublish": "echo 'building!' && npm run build",
  "author": "leighbarnes",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/leighs-hammer/shopify-jwt-auth-verify"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.4",
    "typescript": "^3.9.7"
  }
}
